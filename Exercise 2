#We start by creating an empty dictionary called balls.
balls = {}
#We read input until we receive the "End" command, splitting each line into a color and count using the colon and 
#space as separators, and adding the color and count to the balls dictionary.
# Read input until "End" command is received
while True:
    data = input()
    if data == "End":
        break
    color, count = data.split(": ")
    balls[color] = int(count)
#Once we have all the counts, we calculate the total number of balls by summing all the values in the balls dictionary, 
#and we subtract the largest value from that sum to get the smallest count of balls to remove. We can do this 
#because removing the largest count will leave us with only one color in the box.
# Find the smallest count of balls to remove
smallest_count = sum(balls.values()) - max(balls.values())
#Finally, we print the smallest count of balls to remove.
print(smallest_count)


